name: (BUILD)

on:
  workflow_dispatch:
   inputs:
      version:
        description: 'Select FSCM Version'
        required: false
        default: ''
        type: choice
        options:
        - "*"

permissions:
  contents: read

defaults:
  run:
    shell: pwsh
    
env:
  FORCE_COLOR: 3

jobs:
   Initialization:
    runs-on: [ windows ]
    outputs:
      settings: ${{ steps.ReadSettings.outputs.SettingsJson }}
      type: ${{ steps.ReadSettings.outputs.type }}
      source_branch: ${{ steps.ReadSettings.outputs.source_branch }}
      versions: ${{ steps.ReadSettings.outputs.VersionsJson }}
      environments: ${{ steps.ReadSettings.outputs.EnvironmentsJson }}
      githubRunner: ${{ steps.ReadSettings.outputs.GitHubRunnerJson }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Initialize the workflow
        id: init
        uses: ciellos-dev/FSCM-PS/WorkflowInitialize@v1.1

      - name: Read settings
        id: ReadSettings
        uses: ciellos-dev/FSCM-PS/ReadSettings@v1.1
        with:
          version: ${{ inputs.version }}
          
      - name: Check for updates to FSCM-PS system files
        uses: ciellos-dev/FSCM-PS/CheckForUpdates@v1.1
        with:
          type: ${{ steps.ReadSettings.outputs.type }}
          settingsJson: ${{ env.Settings }}
          SecretsJson: ${{ env.RepoSecrets }}

   Build:
    needs: [ Initialization ]
    runs-on: ${{ fromJson(needs.Initialization.outputs.githubRunner) }}
    strategy:
      matrix:
        version: ${{ fromJson(needs.Initialization.outputs.versions) }}
      fail-fast: false
    name: Build ${{ matrix.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Read settings
        uses: ciellos-dev/FSCM-PS/ReadSettings@v1.1
        with:
          version: ${{ matrix.version }}
          
      - name: Read secrets
        uses: ciellos-dev/FSCM-PS/ReadSecrets@v1.1
        env:
          secrets: ${{ toJson(secrets) }}
        with:
          settingsJson: ${{ env.Settings }}
          secrets: ""
          
      - name: Setup MSBuild
        if: ${{ needs.Initialization.outputs.type != 'Retail'}}
        uses: microsoft/setup-msbuild@v1

      - name: Setup MSBuild
        if: ${{ needs.Initialization.outputs.type == 'Retail'}}
        uses: microsoft/setup-msbuild@v1
        with:
         vs-version: '[15.9,16.11)'

      - name: Setup NuGet
        uses: nuget/setup-nuget@v1
        with:
          nuget-version: 5.10.x    
          
      - name: Run pipeline
        uses: ciellos-dev/FSCM-PS/RunPipeline@v1.1
        id: runpipeline
        with:
          type: ${{ needs.Initialization.outputs.type}}
          version: ${{ matrix.version }}
          settingsJson: ${{ env.Settings }}
          SecretsJson: ${{ env.RepoSecrets }}
      
      - name: Publish artifacts  
        uses: actions/upload-artifact@v2
        with:
          name: ${{ steps.runpipeline.outputs.PACKAGE_NAME }}
          path: ${{ steps.runpipeline.outputs.PACKAGE_PATH }}
          
   PostProcess: 
    if: always()
    runs-on: ${{ fromJson(needs.Initialization.outputs.githubRunner) }}
    needs: [ Initialization,  Build ]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Read secrets
        uses: ciellos-dev/FSCM-PS/ReadSecrets@v1.1
        env:
          secrets: ${{ toJson(secrets) }}
        with:
          settingsJson: ${{ needs.Initialization.outputs.settings }}
          secrets: ""
          
      - name: Finalize the workflow
        id: PostProcess
        uses: ciellos-dev/FSCM-PS/WorkflowPostProcess@v1.1
        with:
          remove_current: ${{ needs.Initialization.outputs.environments == '' }}
          settingsJson: ${{ needs.Initialization.outputs.settings }}
          SecretsJson: ${{ env.RepoSecrets }}
